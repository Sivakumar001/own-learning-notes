#include<bits/stdc++.h>

using namespace std;

bool rabin_karp(string text, string pattern){

}
int main(){
    string text = "abcefd";
    string pattern="cef";

    if(rabin_karp(text, pattern)){
        cout << "found";
    }
    else{
        cout << "not found";
    }
}